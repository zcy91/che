"SELECT k.*, c.coupontype,c.couponname,m.nickname,m.avatar,m.realname,m.mobile,u.merchname FROM `ims_ewei_shop_car_kadata` k  left join `ims_ewei_shop_merch_user` u on k.merchid = u.id  left join `ims_ewei_shop_car` c on k.couponid = c.id  left join `ims_ewei_shop_member` m on m.openid = k.openid and m.uniacid = k.uniacid  where  1 and  k.uniacid = :uniacid ORDER BY usetime DESC"